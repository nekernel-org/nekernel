-------------------
       hCore
-------------------

A C++ based microkernel.

Features:

- It's own microkernel
- It's own format
- It's own bootloader
- It's own filesystem.

Team:

- Amlal El Mahrouss, Lead Kernel Programmer/UI assistant.